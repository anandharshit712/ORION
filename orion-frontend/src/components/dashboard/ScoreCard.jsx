// ORION — ScoreCard  [P1]
// Single metric score card with spark line and CI annotation
// TODO [P1]: Implement this component.

import React from 'react';
import './ScoreCard.css';

/**
 * ScoreCard
 * Single metric score card with spark line and CI annotation
 */
export default function ScoreCard(/* props */) {
  return (
    <div className="ScoreCard">
      {/* TODO [P1]: Implement ScoreCard */}
      <p style={{ color: '#888', fontSize: '12px' }}>ScoreCard — not yet implemented [P1]</p>
    </div>
  );
}
