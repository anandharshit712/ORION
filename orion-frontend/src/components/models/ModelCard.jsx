// ORION — ModelCard  [P1]
// Model name, version, status badge, last run composite score
// TODO [P1]: Implement this component.

import React from 'react';
import './ModelCard.css';

/**
 * ModelCard
 * Model name, version, status badge, last run composite score
 */
export default function ModelCard(/* props */) {
  return (
    <div className="ModelCard">
      {/* TODO [P1]: Implement ModelCard */}
      <p style={{ color: '#888', fontSize: '12px' }}>ModelCard — not yet implemented [P1]</p>
    </div>
  );
}
